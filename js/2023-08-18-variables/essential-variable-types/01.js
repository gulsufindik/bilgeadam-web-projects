/* Primitive Types */

let userName = "Burak"; //string
let userId = 274; //number
let hasPet = false; //boolean
let basketTotal; //undefined
let userPreferences = null;

console.log( typeof userName, typeof userId, typeof hasPet, typeof basketTotal, typeof null  );

//strict typed languages

//loosely typed languages

let userItemCount = 3;

//dynamic typed language
userItemCount = false;





/* Referenced Types */

/* --- collection structures array  --- */
let seasons = ["spring", "summer", "fall", "winter"];

console.log( typeof seasons)

/* --- collection structures object  --- */
let userSettings = {
  theme:"dark",
  notifications:false,
}

console.log( typeof userSettings)
