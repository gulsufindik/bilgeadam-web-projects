// js naming convention : Camel Case

//statement sonunda ; kullanalım

var userName;

var totalPrice = 2000;

console.log(totalPrice, userName);

var result = 3 * 2; /*  expression count ??? */

// = assignment operator,  multiplication * operator, var is reserved

//result = 3 * 2, 3 * 2,

//expressions resolving to values

console.log(result, (result = 3 * 2), 3, 2, 3 * 2);

//js is case sensitive
const userId = 134;
const UserId = 255;
//Non latin karakterler ile adlandırma yapabilrisin
const şey = "şey";
// Değişken İsimlendirmelerinde sayı ile başalama ve tire kullanma
//const deneme-deneme = 3;
